Vagrant.configure('2') do |config|
  config.vm.box      = 'a_menezes/debian9x64' # debian stretch
  config.vm.hostname = 'debian9x64'
  config.vm.provider 'virtualbox' do |v|
    v.memory = 512
    v.cpus   = 1
  end
end
